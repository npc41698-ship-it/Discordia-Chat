Discordia React App
-------------------

This project was auto-generated from your uploaded component.

How to run:

1. Install dependencies:
   cd /mnt/data/discordia_project
   npm install

2. Run dev server:
   npm run dev

Open http://localhost:5173 in your browser.

Note: Firebase is included; make sure your project allows the app's auth and Firestore rules are set.
